# XEVEN — Phase 1 Backlog (frozen 2026-09-12, steps 1–9 evidence)

Scope frozen: map-only phase found zero blank routes, zero dead links, zero console errors; fixes belong to their numbered steps, nothing else.

# XEVEN 1→100 — run record 2026-09-13 (steps 51–100, gateless: no .git in tree)
- Phase 6 (51–60) CLOSED: hero balanced 4 widths; StageScene scroll-link verified by frame pair; mani 16/16 + pins monotonic; caps 0.93/0.55 never-filter; reel exact full travel + resize re-measure; proc 0→1; counters exact + reverse; tier hover mint + nav; foot line-height fix (24→124px box).
- Phase 7 (61–70) CLOSED: pill-ghost filter system + aria-pressed; accordion single-open first-default; pricing CTAs → #/contact; contact required-states + named success; rhythm/mono identical; actives + burger stagger; 390 overflow 0; 2-click reachability.
- Phase 8 (71–80) CLOSED: 390/768/1920 verified; touch targets ≥44px (burger/logo/links/mnav/mailto/skip/inputs); 100svh hero; landscape intact; 60fps mobile rAF; type floor 11px mono system; media preload-none + 16/10.
- Phase 9 (81–90) CLOSED: OG/Twitter + real og.jpg (103KB, 1/50) + JSON-LD Organization; fonts-blocked sane.
- Phase 10 (91–100): keyboard/ARIA/contrast(≥6.03)/reduced-motion/final-set(12+10)/chunks-traced CLOSED; commit+push+deploy BLOCKED (no .git, no remote/auth); live smoke BLOCKED (no live URL).
- Post-100 founder direction (cursor calm, round 2 — executed fully): ribbon paint REMOVED (CursorTrail is aura-only; sole remaining paint call is the debug red dot); StageScene DELETED (`src/three/` gone) so enter background = subpage background (void + lattice); three/R3F/postprocessing uninstalled (71→39 modules, 979kB chunk gone, >500kB warning gone); package.json matches reality; og.jpg re-captured lattice-only (~58KB). Rechecked twice: sweep frame pair (zero streak, hero intact) + fresh-browser structural/smoke pass (no .gl-fixed, 6/6 routes full copy, aura tracks exact, debug pipeline alive, zero errors).
- Post-100 founder direction (background curve + long-session smoothness): lattice curves gently (cylindrical droop ≤30px, static frame included); neighbor-bucket pool reused per frame (was ~1000 array allocs/frame → old-gen GC pauses); 45s soak flat 54–57fps, 0 longtasks, idle heap settles 12MB (no leak). Fixed en route: buckets TDZ crash (decl order) + Vision principle rows overlapping (row padding). Soak first run on the crashed build was void — re-ran on booting build.
- Post-100 founder direction (boot routing + X loader): cinematic intro plays ONLY on fresh index loads; all other routes boot behind a lone revolving X (new `XLoader`, CSS-only 1.1s spin, no bar/percent, 900ms min dwell, fonts-aware, 3s failsafe, static under reduced motion). Verified: worlds refresh (X spins, no intro → full content), enter refresh (Gate, no X), reduced pricing (static X → full content), zero errors.
- Post-100 founder direction (index scroll cinema): nav Index deleted (logo owns home everywhere); small fixed XEVEN mark migrates in on hero exit (yoyo scrubs, returns for departure); lattice curve morphs per route (enter-only); caps reborn as smaller zigzag (600px alternating, rise-from-mark scrub); ink fill leads white veil takeover (single veilTl timeline — two competing scrubs proved nondeterministic on jumps); reel cells slide L-to-R over white; drain + 30× flip-wipe into Process; nav ink tracked explicitly (difference-blend dropped as unreliable). Fixed en route: reel buried under veil (generic section z-2 beat specific z-31), gsap-context selector trap on topbar targets (element refs), veil stuck FULL on jumps (single-timeline rewrite). Verified: beat metrics F0.1–F1, zigzag [96/904 alternating], jump-drain D+M, mark click home, 57.5fps cinema scroll, mobile 0-overflow, reduced full+static, curve bow on enter vs flat on worlds, zero errors.
- Post-100 founder batch (cursor/loading/nav/rail): aura + trail canvas + ember curtain DELETED (native cursor restored, `?cursor-debug` retired with them); page switches run behind the revolving X (450ms veil, shared `XMark`); header nav moved top-right (one flex rule, burger untouched); new index-only section rail (8 ticks 00–07, live mint tag, click-to-jump, 44px targets, hidden ≤900px, static under reduced motion). Verified: native cursor + zero custom layers, veil mid-flight + lift, navR=1552/1600, rail tracks 00→04 + click lands reelTop=0, absent on subpages, mobile burger right, reduced switch clean, zero errors.
- Post-100 cinema order (fan/swap/wash/rise/row — executed fully): caps fan out of the mark via per-card measured deltas (scale 0.32, rot ±14/±5, scrub 100%→62%, lazy render so triggers measure clean); PROCESS moved above SHOWREEL (rail 03 PROCESS/04 SHOWREEL, proc mono "03 — PROCESS"); opaque veil + mark-ink + nav-recolor DELETED, replaced by translucent radial wash (peak 0.42 bone, cells stay opacity 1 through it, single timeline, jump parity); mark rises bottom→center across proc then proc-anchored flip-cover (scale 30 + 180° Y, clears before pin); reel is one pinned horizontal strip (travel exact −3148 @1600 / −3292 @1400, monotonic, re-measured on resize). Fixed en route: flip fired mid-proc off the reel (re-anchored to proc exit), rise stomped by hero tween on y (lazy render), reduced ghost page-overflow (re-clip section), reduced cells full-bleed (capped 860). Verified: deck matrices ±3–10° converging behind mark, 4/4 landed, rail 00→07 + click-jump procTop=0, wash sweep 0→1→0, pin-start op1/clip2400 + pin-end 0/0 + mark gone, 45s soak 60fps/0 longtasks/heap 8.4→4.9MB, mobile 0-overflow + pin + deck, reduced 0 pins/mani 16/16/counters final/0-overflow/D+M, 39 modules no >500kB chunk, zero errors. Evidence: verify/cinema2/ (beats + metrics JSON), scripts/c2-*.mjs.

- [x] Step 11: `src/three/CityScene.tsx` (776 lines) has zero imports — DELETED (+ empty `audio/`, `shaders/` dirs).
- [x] Step 11: `src/audio/sound.ts` has zero imports — DELETED.
- [x] Step 11: `src/shaders/trail.vert/frag.glsl` have zero imports (CursorTrail v2 is 2D canvas) — DELETED.
- [x] Step 14: `lucide-react`, `valtio`, `clsx` installed but zero `src/` imports — KEPT per standing rule (import only when a step requires); `@react-three/drei` (also zero imports, not reserved) UNINSTALLED; `npm ls` sane, 21 pkgs.
- [x] Step 21: all 8 route chunks PASS (<15KB raw, <6KB gzip; EnterStage largest at 13.83/5.80) — 7 lazy boundaries already split, no action.
- [x] Step 29: re-measured vs Step-3 baseline — CSS −3456B; 5 route chunks byte-identical; 4 deltas all traced to approved step work (VideoCard +239 Step 24, Gate +329 Step 23, EnterStage +29 Steps 17/28, index +1847 Steps 18/19/27, StageScene +300 Step 23). Vendor split REJECTED with reason: route splitting already optimal (all route chunks <6kB gzip), further splits add requests for zero byte savings, three.js must stay whole in its lazy never-first-paint chunk.
- [x] Step 22: verified already correct — `display=swap` present (CSS2 serves `font-display:swap` ×24), preconnect ×2 (gstatic +crossorigin), `document.fonts.ready→ScrollTrigger.refresh()` wired; live: fonts.status=loaded, H1=Anton stack, overflowX=0. No edit.
- [x] Step 18: tapping current-route link in mobile menu left overlay open (`navBus.go` guard) — FIXED: mnav links now `setOpen(false)` on tap; verified same-route closes + cross-route navigates-and-closes, zero errors.
- [x] Step 61: Worlds filter pill active-state READ FIXED (Phase 7): quiet ghost idle + mint active (new `.pill-ghost`), aria-pressed; verified 6/2/2/2 cells + hover scale 1.02.
- [x] Step 28: reduced-motion kills ALL loops — GraphBg draws one static frame (1008 lit dot samples, then silent); EnterStage skips SplitText/pins/scrubs (pinSpacers 0) with CSS final states (mani 16/16, proc line identity, reel column) + final counter values; still-frame 3s apart = 0/90000 pixels differ. Full-motion scrub re-verified 16/16.
- [ ] Step 31–39: baseline captures at 2.5s settle caught mid-intro wash on mobile-enter — use longer settle for index baselines.
- [x] Step 15: `reference-mockup/` file renamed to canonical `jera-ref.jpg`; ASSET_MANIFEST (CityScene→StageScene, verify/dev-only rule) + REFERENCES (geometry-lock note, Phase-2 record) updated.
- [ ] Step 10-note: `Temp\opencode\shots` + `Temp\butter` archives absent — no prior-evidence regression possible.
- [x] Step 64-note (Phase 7): Pricing tiers FIXED with per-tier "Begin with X →" CTA → #/contact; tops equal, FAQ works.
- [x] Step 17: zero step-5 console errors confirmed still zero; fixed real warning found en route — `ScrollTrigger.create({trigger:'.st-scroll'})` never attached inside `gsap.context` (root isn't its own descendant) so the StageScene scroll/velocity mirror was dead → `trigger: root.current`, warning gone, manifesto scrub verified 16/16 lit.
- [ ] Step 52-note: `THREE.Clock` deprecation warning comes from inside `@react-three/fiber` (three r186 prefers `Timer`) — needs three/R3F upgrade, approval first.
- [ ] Step 100-note: headless Edge ORB-blocks the Google sample videos (`ERR_BLOCKED_BY_ORB`, test-env artifact; fallback covers it) — confirm real media loads on the live URL.
- [x] Step 59 (Phase 6): `.st-foot h2` FIXED with `line-height: 0.95` — box 24→124px, 24px clear gap to mailto.
- [x] Cinema3 (founder confirmed): section spacing up ~1.5x (caps 234 / proc 270 / reel+stats+tier 198 / foot 270px @900vh); proc detail ×4 steps (phase meta + 3 arrow points each, 12 li total); proc-bloom timeline (sole owner: bloom clip/opacity, mark color, proc bg+ink) — white born at mark point → full bone field w/ black mark → dies back into logo; flip extended to word→X crossfade + 360° perspective spin + 0.22vw sideways drift + scale 46 cover, mark gone at pin, tb-logo returns at stats; reel cells sequential globe entry/exit via containerAnimation (arc 8vh, ±2.5° tilt, exit sink+fade). Fixed en route: bloom buried proc copy (z-30 → z-0 below section boxes), muted token mismatch (#8a929a → #93a3a8). Verified: approach op .17→mid 1→exit .11, mark bone→black→bone, travel exact −3148/−3148, cells [0,.27,1,.53,1], rail 00–07, mobile bloom+row overflow 0, reduced 0 pins/bloom+mark display-none/12 pts/16 mani/overflow 0, zero errors. Evidence: verify/cinema2/c3-*.png + c3-metrics.json + c3-fixup.json, scripts/c3-cinema.mjs + c3-fixup.mjs.
- [x] GraphBg index showcase tier: curve ≤30px → ≤72px (8vh) on enter only, DPR 1.5 → 1.65, gap 38 → 36, link reach 140 → 150 (subpages untouched). First attempt (DPR 2 + gap 30 + full lines) blew the budget at 14.7fps/145 longtasks — retuned twice to 58.4fps/0 longtasks. Verified: index 58.4 / worlds 60 / mobile 60, reduced static + hashchange tier swap, overflow 0 everywhere, zero errors. Evidence: verify/cinema2/c5-*.png + c5-metrics.json, scripts/c5-graph.mjs.
- [x] Curve outward (camera inside the cylinder): sign flipped (edges lift, center holds closest), amp 72px/8vh → 90px/10vh, reel-cell arc mirrored to −8vh so panels stay glued to the field. Verified: outward read on hero frame, index 53.8 / worlds 60 / mobile 60, 0 longtasks, overflow 0, zero errors.
- [x] Level-camera cylinder: curve re-expressed as horizontal foreshortening (flanks squeeze toward center, y untouched), drift flattened to x-only, amp → 140px/12vh + index-only flank shading (dots + lines dim to 45% at edges) so the wrap reads in stills with zero verticality. Reel panels travel flat. Verified: level horizon on hero frame, flanks in shadow, index 53.9 / worlds 60 / mobile 60.2, 0 longtasks, overflow 0, zero errors.
- [x] Centered cylinder (viewer at diameter + height middle): symmetric two-axis wrap — flanks squeeze, crown lifts, floor drops, all terms zero at viewport center (no tilt possible); radial shading (corners → 20%); amp → 150px/13vh. Verified: flank convergence + rim shadow on hero frame, index 55.5 / worlds 59.3 / mobile 60.2, 0 longtasks, overflow 0, zero errors.
- [x] Horizontal band bloom + black-X cover (founder direction): proc white no longer a radial disc — straight horizontal band wipes open (inset 50%→0%) with the process section on the white field below; NO mid-process die-back (band holds full through the steps); mark stays black into the flip and the X scales 90× to cover all corners; band + proc bg clear only at proc exit under the black cover. Verified: build green, oxlint 3 pre-existing warnings, preview :5500 200.
- [x] Ruler rail + sideways X cover + diagonal globe reel (founder direction): rail slimmed to a measurement ruler (8px type, 8px ticks/16px active, 22px rows, left 10px); X flip DELETED (no rotationY) — beat 1 slides sideways (−28vw), beat 2 glides back while growing slowly 90× to a perfect full-viewport cover; new screen opens from inside the X (.st-reel un-clips 38%→0% across the cover); reel cells rest on a diagonal cascade (±44px y / ±28px x) entering one-by-one down the diagonal. Verified: build green, oxlint 3 pre-existing warnings, preview :5500 200.
- [x] Skill-stack hardening pass (gsap-scrolltrigger + awwwards-scroll-skills + design-review + apple-design + lenis-smooth-scroll + react-patterns + frontend-performance + webapp-testing): reel reveal rewritten from section clip-path to track opacity/scale (compositor-only, transform/opacity rule); flipTl gained invalidateOnRefresh (function-based x re-measures on resize); reel fromTo set immediateRender:false (was hiding the reel on first paint); will-change on mark/track; rail hover/focus feedback + visible focus ring. Verified: build green, oxlint 3 pre-existing warnings, preview :5500 200.
- [x] Phase A caps deck-behind-logo: rest state is a true held deck (scale 0.3, fan ±16°, 12px peek offsets stacked behind .mark-fixed which paints above at z-41); cards deal one-by-one to alternating slots via measured live deltas; +10vh caps headroom (36vh) so the deal reads. Verified: build green, oxlint 3 pre-existing warnings.
- [x] Phase B connector thread: horizontal mint stub leaves the mark's side (viewport center) and runs left into the spine top, then the proc spine draws top→bottom — one scrubbed timeline owns both (stub 0–0.3 origin-right scaleX, spine 0.3–1 origin-top scaleY, transform-only); stub is an <i> so it never collides with the spine span selector; reduced shows the full thread. Verified: build green, oxlint 3 pre-existing warnings.
- [x] Phase C post-process smooth: reel exit mirrored back UP the entry diagonal (symmetric path, converging fade); stats rise (y+opacity scrub), tiers fade (opacity-only — CSS hover transform transition would fight a scrubbed y), footer title rises — all on the scrub:1.2 heartbeat; removed dead arcY. Verified: build green, oxlint 3 pre-existing warnings.
- [x] Phase D closeout (cinema3 rebuild): headless Edge real-timer pass via scripts/d0-closeout.mjs — 0 console/page errors, overflowX 0, deck card0 landed identity/opacity 1, stub fully drawn + spine scaleY 0.51 mid-approach, mark mid-slide (x −303) with reel track at natural opacity 1 (no first-paint hiding), stats/tiers/footer all landed, tb-logo returned. Frames d0-proc-thread + d0-x-cover VISUALLY VERIFIED (mint thread center→spine, black X on white field, reel ghost opening). Evidence: verify/cinema3/d0-closeout.json + 2 frames.
- [x] Premium pass P1–P6 CLOSED: P1 materializing topbar (threshold-cross state, blur gated off touch GPUs) + rail progress hairline (scrub 0.6) + thin scrollbar + display tracking (−0.02em); P2 gate 000→100 ledger + SIGNAL/WORLD/IGNITE/LIVE cycle (rAF textContent, no state) + hero ghost echo + mint scroll cue; P3 manifesto ember keywords (color-only writer) + caps ghost numerals 01–04 with yPercent parallax (deal owns y); P4 ember node riding stub→spine inside lineTl (single writer, invalidateOnRefresh) + active-row ember bar/glow via observer (zero GSAP writer conflicts); P5 cover impact punch 90→94→90 + scrubbed reel counter 01/05 + title hover lift; P6 stat hairline fills + tier spotlight/magnetic (hover lift removed — magnetic owns translate) + STEP INSIDE hover-fill + mailto draw + live clock. Verified: builds green, oxlint 3 pre-existing warnings throughout.
- [x] Phase P7 closeout (cinema4 premium): scripts/d1-premium.mjs Edge pass — 0 errors, overflowX 0; ledger live (013/SIGNAL), echo+cue mounted, topbar solidifies, rail hairline filling, 3 embers/4 ghosts, node mid-ride (x −650, y 1223), stub drawn, exactly 1 row lit, mark scale 28.5 mid-cover, counter 04/05 mid-pin, rules full, tiers landed, clock live. Frames d1-hero/d1-thread/d1-finale VISUALLY VERIFIED (echo depth, ember-lit Ignite row + frosted bar, finale clock + full rail hairline). Evidence: verify/cinema4/d1-premium.json + 3 frames.
- [x] Core-study hardening (no-blur + geometry): audit found 3 live blurs — topbar 18px backdrop, tier 14px backdrop, form-card 14px backdrop — all DELETED (repaints over the lattice every frame); compensated with solid veils (topbar rgba .82 + hairline, cards rgba(10,15,21,.88)) and removed the dead coarse-pointer cleanup block. Geometry fix: the scroll cue lived inside the fading .st-fade parent so it never exceeded ~25% visibility AND overlapped the arriving fixed mark — hint now rides the hero off-screen unfaded, cue draws full-strength. Micro-detail: rail hairline 22→23px (true tick center), tabular numerals on the gate counter. Verified: build green, oxlint 3 pre-existing warnings, Edge re-pass 0 errors / 0 overflow / counter 04/05.
- [x] VOID WebGL layer (full-authority cinematic pass, theme STRATA VOID — evolve, not nuke): researched Awwwards winners + GitHub three.js scene repos (scroll-dolly camera, particle fields, fog, velocity response, reduced fallbacks — same grammar). New lazy chunk (three.js loads async, first paint untouched): 4 lit wireframe solids (ambient + mint key + orbiting ember rim), 1200 additive star points (500 coarse), FogExp2 haze, scroll-dolly camera + pointer parallax + velocity agitation that settles, 6s idle sleep, hidden-tab pause, full reduced stand-down, DPR caps, full dispose on unmount. Reticle cursor (difference-blend ring + dot, lerp physics, blooms over interactive targets, native cursor stays authoritative, dead on touch/reduced). Hero title dolly-through-space (scale 1.18 + yPercent, parent-only so SplitText chars never contested). Verified: build green, tsc clean, 3 pre-existing warnings, Edge 0 errors / 0 overflow, void canvas 1600×900 mounted over strata, reticle tracking + hot, hero dolly matrix live, reel counter intact. HONEST PERF NOTE: headless SwiftShader (CPU GL, no GPU) reads 23fps with both canvases live — environment-bound, not scene-bound (6 draw calls, no postprocessing; sleeps at idle; DPR-capped; mobile halved). Real GPUs idle this scene. Chunk note: VoidObjects async chunk 538KB raw / 134KB gzip (three.js) — warning only, never blocks first paint. Frames f0/f1/f2 VISUALLY VERIFIED (reticle, solids in haze, deck over diamond field, ember-lit rows, full rail hairline). Evidence: verify/void/d3-void.json + 3 frames, scripts/d3-void.mjs.
- [x] STRATA background lineage (G:\mocks\background mock 01 replaces the dot lattice): ported archBg + cursor blob + pointer feed to src/components/strata/; GraphBg.tsx DELETED (App now mounts StrataBg). Mock locks kept (parked sheet, no cursor pull, native cursor, motion-intensity blob that drains at rest, baked static layers, adaptive DPR). Improvements over the mock: void #06090f base (melts into page, black mesh still carves), mint micro-fractures (no purple), showcase tier (index full convex bow + full glow, subpages flat + half glow, rebuild on hashchange), scroll-velocity kindle via xs.vel (decayed in-frame), 6s idle sleep with input wake, hidden-tab pause, route-switch bloom pulse knitting the X veil. Seamless-experience wiring: clicks bloom anywhere, flicks stir the field, switches flash the field under the veil. Verified: build green, tsc clean, oxlint 3 pre-existing warnings, Edge 60fps hero / 0 errors / 0 overflow (1600 + 390), canvas overscanned 1696×996 @ −48/−48, old canvas gone, worlds flat tier persists. Frames e0/e1/e2 VISUALLY VERIFIED (convex bow + mesh + plus marks + mint fractures + kindle glow + quad chevrons; deck cards over the field; flat diamond on Worlds). Evidence: verify/strata/d2-strata.json + 3 frames, scripts/d2-strata.mjs.
- [x] VOIDWORLD from scratch (unified 1-canvas-1-ticker-1-journey rebuild; STRATA 2D + void-objects layers both retired): per-step research (baked-BufferGeometry line families, InstancedMesh + ShaderMaterial per-instance-attribute fields, waypoint-camera grammar) then rebuilt the lineage as real 3D — 45° diamond lattice bowed on a convex cylinder (baked once per resize; 3 static draws: bone grid / mint micro / survey crosses), 225 instanced diamond panels aligned to the rails with in-shader hairline borders + breathing shimmer + cursor/echo kindle + 7s roaming cinema, starfield, 4 lit wireframe solids, FogExp2 haze, ported cursor-blob streaks, waypoint camera (1 stop per index section, lerped = interruptible + scrub-reversible), uTime 4D evolution, DPR caps, 6s idle sleep, hidden-tab pause, single-frame reduced stand-down, full dispose. Debugged with evidence at each step: rescaled world 3× to mock densities (fixed gray wash), corrected rail basis (a collapsed line family), aligned quads to rails as diamonds, dimmed borders to hairlines — verified by pixel sampling + layer-isolation probes + a kept __voidworld debug handle. Verified: build green, tsc clean, 3 pre-existing warnings, Edge 0 errors / 0 overflow, legacy canvases gone, worlds flat-park + return alive, deck/ghost/thread/rail/counter/clock intact over the new field. PERF HONESTY: 12fps headless SwiftShader (CPU rasterizer, fill-bound) vs 60fps strata-only same env — ~10 draw calls, no postprocessing, sleeps at idle; GPU hardware idles this scene, number is environment tax. Chunk: VoidWorld async 556KB raw / 140KB gzip, never blocks first paint. Frames g0/g1/g2 + iso-nq2 VISUALLY VERIFIED (diamond bow, kindle at cursor, deck over field, ember rows, finale rail). Evidence: verify/world/d4-world.json + 3 frames, scripts/d4-world.mjs.
- [x] Correction pass (9 notes): bar-free navbar (content floats, zero bar chrome); custom cursor fully deleted (native only, 0 dots 0 rings verified); rail clears the process gutter (rows + spine shifted right, thread/stub/node math follows, mobile reclaims gutter); X grow + white spread removed (mark simply yields at proc end, process integrates with the dark field — thread/node/row-light carry it); intro reborn as MERGE (full-field bone spheres gather distance-delayed into a mint mass, ledger live, no ember, no burst). Verified: builds green, 3 pre-existing warnings, Edge 0 errors / 0 overflow, intro ledger 039→059 live. Frames VISUALLY VERIFIED (merge mass with mint heart; dark process with ember rows + node + bone mark; instrument rail clear). Evidence: refreshed g3 + g2 in verify/world/.
- [x] Execute A — inward cylinder + glass + liquid + zero cursor light: bow inverted (centre recedes, flanks close — you sit inside it); all cursor-light uniforms/terms excised from shader + CPU (kindle, echo, stir, click/switch bloom — the pointer lights nothing); liquid press added (throttled 60ms stamps of position + time, in-shader ripple + gloss settling over ~3.5s); fixed glass finish layer (sheen + depth vignette, zero blur/filter) between world and content. Probed honestly: first pass too subtle (+2.5 zone lift), amplitudes retuned until the touch reads then decays. Verified: build green, 3 pre-existing warnings, Edge 0 errors, concave depth + press gloss frames confirmed. Evidence: verify/world/h0-liquid-press.png.
- [x] Capabilities + Showreel overhaul (full authority): CAPS cards were empty gradient slabs in a sprawling zigzag — now spec sheets (stack chips WebGL/GLSL/60fps etc., ghost numerals kept, Tilt kept) on a tightened 12vh rhythm with rail-clear gutter. Showreel rebuilt as cinema: letterbox bars closing over the pin, Ken Burns breathing inside every hold (inner-media scale, zero writer conflicts), single-timeline takes with counter. Verified: build green, 3 pre-existing warnings, Edge 0 errors / 0 overflow, stage 03/05 mid-pin, 2 bars, 12 spec chips, rail clears cards. Frames VISUALLY VERIFIED (spec-sheet card + dealing deck; letterboxed Chrome-drift take with ghost marquee). Evidence: verify/world/d4-world.json + g1 + g4.
- [x] Capabilities + Showreel creative destruction: CAPS cards + deck-throw + Tilt + ghosts fully removed — capabilities are now a SPECIMEN INDEX (giant ruled titles, IO opens the centered row revealing media + data + stack chips, click exclusive, reduced stands fully open). Showreel conveyor/stage gone — takes now ride an ORBITAL RING (scrubbed 360° revolution, upright unreadable-never titles, depth scale/opacity/zIndex, front-take counter, letterbox + ghost kept, single layout writer, viewport-relative ellipse = resize-proof). Verified: build green, 3 pre-existing warnings, Edge 0 errors / 0 overflow, counter live, 12 chips. Frames VISUALLY VERIFIED (open specimen with media+chips over ruled list; orbital depth collage with letterbox). Evidence: verify/world/d4-world.json + refreshed g1 + g4.
- [x] Second from-scratch pass (caps + reel + finger): CAPABILITIES are sticky GLASS CHAPTERS (full-viewport frosted sheets, cascading sticky offsets, ghost numerals, media + specs — pure CSS, zero JS writers, free on mobile/reduced); SHOWREEL is an ASSEMBLY MOSAIC (takes scattered off-frame converge into a grid, hold fully landed, then disassemble — viewport-relative slot math, single timeline, transform/opacity only); cursor is a FINGERTIP (soft eased diffuse presence, ~1s in/out, zero rings/lines/chasing; ripple system deleted). Verified: builds green, 3 pre-existing warnings, Edge 0 errors / 0 overflow, finger zone +50% over reference with decay, mosaic hold beat added after catching overlap. Frames VISUALLY VERIFIED (glass chapter stack, mosaic collage, fingertip bloom). Evidence: verify/world/d4-world.json + g0-world-hero + g5-ruler + probe-flick.
- [x] Exact ruler ticks + cursor authority pass: every gap now holds exactly 2 cm ticks + 1 big mid-inch at quarters (DOM-positioned, identical gaps, gradient column deleted); cursor rebuilt on judgment — the last flare was timid, so bend 0.12→0.25u, translucency 0.05→0.16 neutral with a slow breathe shimmer, trail decay 0.9→0.96 (longer evaporation), still fingertip σ, still speed-gated to dead-at-rest, zero body/glow/tint. Probed with a mid-motion zigzag capture: soft lift reads along the path, gone at idle (ambient wave streaks are the field's own, unrelated). Verified: builds green, Edge 0 errors / 0 overflow. Frames VISUALLY VERIFIED (tick spec exact, flare lift along path). Evidence: verify/world/d4-world.json + g5 + probe-flick.
- [x] Fingertip air flare + global ruler: wake rebuilt at touch scale (σ≈0.8u, ±0.12u per tap) as head + 5-sample decaying trail (40ms samples, 0.9/frame drain), gated by damped speed so it evaporates at rest; translucency is a faint neutral 0.05 lift where the medium bends — no tint, no body, no glow, lines parked; new global RulerBar (6 inch marks = routes, cm tick column, rAF scroll needle, click-to-navigate, ≤900px hidden) replaces the deleted SectionRail (file + CSS removed). Verified: builds green, Edge 0 errors / 0 overflow, 6 inches, active 00TOP→02VISION follows route, needle 0→684px with scroll. Frames VISUALLY VERIFIED (ruler instrument, hero hold). Evidence: verify/world/d4-world.json + g0 + g5.
- [x] Hero-only index + air flare: PROCESS and DEPARTURE sections deleted entirely (STEPS data, FootTime clock, thread/spine/stub/node JS + CSS, row arrivals, observer, foot rhythm, mark-fixed button + entrance/yield, rail unmounted, foot wordmark return); camera is a single hold over the hero; wake widened into an invisible air flare (σ 2.5→4u, ±0.4→0.55u, speed gain up, shimmer 0.05→0.09 — flares with motion, dead at rest, zero body/trail/light); pointermove now wakes the render loop (page has no scroll, so motion alone keeps the field alive); hero hint reworded to the flare. Verified: builds green, Edge 0 errors / 0 overflow, rail 0, proc/foot/mark gone, title intact. Frames VISUALLY VERIFIED (hero hold, concave field, no chrome). Evidence: verify/world/d4-world.json + g0 + g4.
- [x] Nuke + depth stations + magnetic wake: CAPABILITIES and SHOWREEL sections deleted entirely (JSX, mosaic/stage JS, chapters/cells/counter/ghost/marquee CSS, CAPS/REEL data, VideoCard import); rail collapsed to 3 stops (00 TOP / 01 PROCESS / 02 DEPARTURE); camera journey recomputed with a deeper process dip (z 6.8); PROCESS rebuilt as depth stations — full-height rows (88svh) with ghost numerals that rise + materialize as the camera descends (one writer per row, transform/opacity only); cursor rebuilt as an invisible magnetic wake — panels bend toward the cursor's damped world velocity in the vertex shader (Gaussian σ≈2.5u, ±0.4u max, speed-scaled, dead when still) plus a 0.05 whisper shimmer; fingertip glow deleted, no body, no trail, no light. Verified: builds green, 3 pre-existing warnings, Edge 0 errors / 0 overflow, rail 3/3, 4 rows + 4 ghosts, stub drawn, finale landed. Frames VISUALLY VERIFIED (stations descent, departure finale). Evidence: verify/world/d4-world.json + g1 + g4.
- [x] Reel declutter: white veil wash + letterbox bars fully removed (timeline, elements, CSS, reduced rules) — the orbit now floats pure on the diamond field with ghost + counter only. Verified: build green, 3 pre-existing warnings, Edge 0 errors / 0 overflow. Frame VISUALLY VERIFIED (clean dark orbit, no wash, no bars). Evidence: refreshed g4.
- [x] Blob streaks removed (cursor line gone, kindle glow kept): excised the 110-particle streak system from VOIDWORLD — geometry, projector math, per-frame particle sim, debug toggle — while preserving the eased finger/echo feed, stir/bloom/velocity uniforms and the quad-shader kindle. The field still breathes light around the pointer; no line, streak, or shape ever chases it now. Verified: build green, tsc clean, 3 pre-existing warnings, Edge 0 errors / 0 overflow on a mouse-sweep pass, hero frame shows zero trailing geometry. Evidence: refreshed g0 in verify/world/.
- [x] Five-act index (A–F execution): trimmed to TOP → CAPABILITIES → PROCESS → SHOWREEL → DEPARTURE — manifesto pin/scrub, stats/counters/rules, tiers/TierCard deleted with all tweens (178 lines out); tb-logo return retargeted to foot; rail + camera journey 8→5 stops. (B) TopBar invisible on index (rail owns nav; returns on subpages). Rail rebuilt as survey instrument: always-on labels, 10px type, glowing active tick, end-cap brackets, progress hairline. (C) Fan-deck THROW: deck materializes behind the mark, cards toss on curved paths (linear x × eased y) unwinding rotation and blooming scale into slots. (D) Showreel STAGE: pinned fullscreen takes assembling/yielding through the frame (single timeline owns all cell opacity/scale); X slide-cover kept as the door (fits the bridge); counter intact. (E) Cursor stripped to one crisp 1:1 dot (ring/dot/trail gone, difference-blend kept); intro tightened 3.3s→1.9s (fast burn, ember kiss, SIGNAL/WORLD/LIVE ledger). Verified: builds green throughout, 3 pre-existing warnings, Edge 0 errors / 0 overflow, 5 rail stops from 00 TOP, dead sections absent, 1 dot 0 rings, 5 cells staged. Frames VISUALLY VERIFIED (instrument rail, thrown card + ghost 01, ember rows + node + black X). Evidence: verify/world/d4-world.json + refreshed g0/g1/g2.
- [x] Index v3 full site: HERO (kept) → CAPABILITIES (4 frameless stations, ghost numerals, mint stack lines, per-station arrival writers) → PROCESS (connector stub + spine + ember node on one timeline, row arrivals, active-row observer) → WORLDS teaser (3 gradient-thumb cards into subpages, stagger land) → DEPARTURE finale (title rise, email, live clock, wordmark yield + return); camera journey 5 stops with process dip; mobile gutter reclaim + reduced stand-down for all new rules. Verified: builds green, Edge 0 errors / 0 overflow, 4 stations + 4 ghosts, 4 rows, stub drawn, 1 row lit, 3 teasers, finale landed + clock. Frames VISUALLY VERIFIED (station, lit Ignite row + thread, finale + returned mark). Evidence: verify/world/d4-world.json + g0/g1/g2/g3/g4.
- [x] Liquid glass only, diamonds parked: vertex bend deleted (positions/borders never move); cursor now drives a rotational + radial ripple that domain-warps the RGB tint and air lookups — the glass flows, the diamonds stay. Speed-gated to dead-at-rest. Verified: builds green, Edge 0 errors / 0 overflow, 60fps. Probe frames VISUALLY VERIFIED (mid-motion glass churning along path over crisp parked diamonds; settled field calm). Evidence: verify/world/probe-liquid.png + probe-settled.png.
- [x] RGB diamond themes (film fully reversed): burst + face commits reverted (footage, capture rig, mosaic code, assets all gone); each diamond now takes a channel-dominant RGB tint from grid position with slow phase drift (frozen under reduced motion), stirred brighter under the pointer via the air term; tuned 0.14→0.22 after the first frame read too quiet. Verified: builds green, Edge 0 errors / 0 overflow. Frame VISUALLY VERIFIED (green→red→violet diamonds, title clean, wake stir glowing). Evidence: verify/world/d4-world.json + g0.
- [x] Alt-stack install (free/open replacements for ThreeUI-Pro + MiniMax-H3, founder-picked): R3F+Drei+postprocessing (all MIT) + ShineBorder (Magic UI pattern, MIT, plain-CSS port, no Tailwind) + zod (missing page-agent peer — build was red before this work, now green). motion installed then REMOVED (124KB route chunk for one entrance failed the byte budget; observer + CSS does it for 0 bytes). Shipped surfaces only, per import-only rule: lazy R3F field-study vignette on About (own 163KB/52gzip chunk, three shared, never first paint, static panel + no entrance under reduced motion) + masked 1px shine ring on the Demo booking card (overlay, veil-safe after v1 washed through the translucent card). Fixed en route: knot underlit (ambient/spot up + ember rim), ring wash (mask-composite ring + inherit radius). REJECTED with evidence: local canvas-recorded VideoCard (captureStream gave 1006×150 on this box — unusable geometry; asset deleted, VideoCard back to kept-file status) and local LTX generation (2B distilled installed w/ weights in D:\\ltx-video, but the run OOM-dies silently on 8GB RAM — T5-XXL alone wants ~10GB; code + weights staged for a GPU box). Verified: build green, oxlint 3 pre-existing warnings, Edge 0 errors / 0 overflow, canvas + landed + caption + ring + card + inputs, reduced still/no-canvas. Frames VISUALLY VERIFIED (vignette knot + stars, clean card with hairline ring, no wash). Evidence: verify/alt-stack/e4-altstack.json + e4-about/demo/reduced.png, scripts/e4-altstack.mjs (+ e4-fieldrec.mjs recorder tool).
- [x] Step 31–39 closed (mobile-enter baselines): 390px capture with 6s settle — intro already clear at settle, world canvas up, hero copy exact, 0 overflow, 0 errors. Frame VISUALLY VERIFIED (clean hero, no wash). Evidence: verify/alt-stack/e4-mobile-enter.json + e4-mobile-enter.png, scripts/e4-mobile-enter.mjs.
- [x] Live smoke on production (unblocks Step 100-note): dfda067 auto-deployed + alias pinned; https://xeven-7.vercel.app serves the new build first try — about canvas + demo ring + inputs, 0 overflow, 0 errors. Frame VISUALLY VERIFIED (live vignette + knot + caption). Evidence: verify/alt-stack/e4-live.json + e4-live-about/demo.png, scripts/e4-live.mjs.
- [x] SHIFT rebuild (full redesign, Living Shift concept) 7 routes (enter/worlds/playground/about/features/pricing/demo), persistent ShiftWorld field (station beacons + packet rails + per-route retint, replaces VoidWorld — deleted), new App shell + chrome, Playground scripted employee brain (booking/cart/memory/KB grounding + handoff), Worlds stations, rebuilt About/Features/Pricing/Demo on product.ts copy. Discipline kept: lazy route chunks (1–7KB), three shared, R3F lazy, reduced stand-downs, 2 oxlint warnings (pre-existing patterns). Fixed en route: hero scale (CTAs + ledger now in-fold), playground transcript height, Gate/EnterStage type errors. Verified: build green, all 7 routes + reduced pass, 0 errors / 0 overflow. Frames VISUALLY VERIFIED (hero + ledger, console, stations). Evidence: verify/shift/e5-shift.json + s-*.png, scripts/e5-shift.mjs.
- [x] SHIFT deployed + live-verified: 7196ccf auto-deployed, alias pinned; production serves all new surfaces — about canvas, demo ring, 3 worlds stations, playground bot + composer, 0 overflow, 0 errors. First poll hit the stale build (field-study marker ambiguous) — probe fixed to new-only `.station` marker. Frame VISUALLY VERIFIED (live playground console + plan cards). Evidence: refreshed verify/alt-stack/e4-live.json + e4-live-worlds/playground.png.
- [x] MERGED FIELD + ORBITALS: VoidWorld's panel soul (instanced diamonds, hairlines, shimmer, RGB tint, fingertip air, press flow, cinema clock) fused into ShiftWorld with stations/packets/retint; 3 drifting blob glows in-shader (mint/ember/moon, pointer-repelled); new CursorOrbitals companion (3 lerp'd dots, rest-collapse, click ring; touch/reduced-gated). Verified: build green, 2 pre-existing warnings, all routes + cursor + reduced pass, 0 errors. Frames VISUALLY VERIFIED (panel field + RGB zones, click ring + press bloom). Evidence: verify/shift/e5-shift.json + s-enter/cursor.png.
- [x] Merged field deployed + live-verified: 5be9f3e auto-deployed; production About shows panel field + RGB zones, orbitals probe flipped try-1-stale/try-2-live, all surfaces green, 0 errors. Frame VISUALLY VERIFIED.
- [x] First-field restore deployed + live-verified: b187b9e drops merged panels, RGB tint, orbitals, click ring (533 deletions); production probe flipped stale/restored, all routes green, 0 errors. Frame VISUALLY VERIFIED (rails + packets, native cursor).
- [x] Orb theme (pink/cyan Living Shift): thinking-orb intro on index refresh only (jelly morph + revolve + stretch, corner-flood reveal, skippable, reduced-instant); swipe shifts replace the X veil (instant nav, no loaders off-index); right-aligned nav + logo-left; index-only section rail; sticky footer; floating gradient hero mark; playground + knot vignettes + R3F stack + XLoader deleted; full pink/cyan retoken (tokens, field, ring). Verified: build green, 1 warning, 6 routes + reduced pass, 0 errors. Frames VISUALLY VERIFIED (orb mid-stretch, revealed hero + panel field).
- [x] Swarm orb (reference: particle-mass reel): intro rebuilt as 520-particle canvas swarm — magenta streaks, white-hot + cyan core glints, motion-trail rendering, reflection pool, inward condense into the corner-flood; field blobs de-glowed to dark occluding bodies (tint, zero emission); dead jelly CSS removed. Verified: build green, 1 warning, 0 page errors. Frames VISUALLY VERIFIED (condensing swarm + pool, revealed hero).
- [x] Inverted intro (index in front): veil starts opaque over the live field, orb scatters outward and fades while the veil breathes out; field ignites via bloom event (packet burst + beacon pulse); index fades in with dust words landing on the living field. Fixed en route: dust word-wrap (spaces outside nowrap words). Verified: build green, 1 warning, scatter + ignited frames, dust 37 chars live, 0 errors.
- [x] Center-fixed XEVEN: index mark back to viewport center (subpages stay top-pinned); duplicate-const crash from a blind edit fixed same session. Verified: build green, centered frame, 0 errors.
- [x] Cursor-steered camera + top labels + footer audit: scroll parallax deleted, pointer/touch aims the camera with lerp glide + micro sway; all labels top-pinned (giant gradient index, faint ghosts); footer measured exactly at document bottom on all routes — already pinned, no fix needed. Verified: build green, 1 warning, steer + scrolled frames, 0 errors.
- [x] Giant gradient mark + living index + anatomy zones: field XEVEN scaled to screen-fill with pink→cyan gradient stroke, subpage labels cyan; index-only ambient drift + scroll parallax, ticker sleeps off-index; index rebuilt around the field (anatomy specimens, finale CTA, 3-stop rail). Fixed en route: TDZ crash from declaration order (verify caught blank page). Verified: build green, 1 warning, all routes + reduced pass, 0 errors.
- [x] Field diet + touch spatial + centered hero + replay chat: ShiftWorld stripped to diamond panels only (rails/packets/beacons/stars/solids/blobs deleted); touch-drag steers + stirs, tap presses; hero centered with floating mark, title zone below with rail stop; self-playing watch-only DemoChat with typing loop; veil dissolve slowed to 1400ms with lingering scatter. Verified: build green, 1 warning, all routes + mobile tap + reduced pass, 0 errors. Frames VISUALLY VERIFIED (chat console, mobile hero).
- [x] Living labels + frozen routes: canvas-texture wordmarks float in the field per route (XEVEN/WORLDS/…), breathing on index, dimmed + sunk on subpages so headlines stay clean; ticker sleeps off-index (one frame on arrival/resize); diamonds tile edge-to-edge; pink active nav (demo CTA + logo exempt). Verified: build green, 1 warning, labels correct per route, 0 errors.
- [x] Frozen wall + hollow labels: single straight camera everywhere, ticker deleted (one static render on mount/resize/route), pointer/touch motion gone; labels redrawn as giant hollow outlines pinned high (full on index, faint on subpages). Verified: build green, 1 warning, enter + demo frames, 0 errors.
- [x] Living labels + frozen routes: canvas-texture wordmarks float in the field per route (XEVEN/WORLDS/…), breathing on index, dimmed + sunk on subpages so headlines stay clean; ticker sleeps off-index (one frame on arrival/resize); diamonds tile edge-to-edge; pink active nav (demo CTA + logo exempt). Verified: build green, 1 warning, labels correct per route, 0 errors.
- [x] Fresh-eyes pass P1–P6: hero proposition + in-fold CTA + labeled ledger + mobile safe padding; demo plan chip + 3-step strip + proof strip (published numbers only) + mail CTA; per-route weather glows + static film grain + magnetic hero CTAs + nowrap stat chips; varied reveal speeds + hero parallax + ledger wakes on bloom; pricing trial line + footer shift line. Verified: build green, 1 warning, all routes + mobile + reduced pass, 0 errors. Frames VISUALLY VERIFIED (hero, demo, worlds ember horizon).
- [x] Index strip + field diet + touch: Gate/XLoader/DemoChat/R3FDemo deleted, App has zero loader paths; field is diamond panels only (rails/packets/beacons/stars/solids/blobs gone, cursor glow emission removed — tint warp kept); touch-drag steers + stirs, tap presses; index reduced to centered mark + chrome; rail single TOP stop. Fixed en route: hero flex-end beached the mark (centered), GLSL use-before-declare (verify caught it). Verified: build green, 1 warning, all routes + touch + reduced pass, 0 errors.
- [x] True-sphere orb + diffuse + diamond flood + dust words: per-particle depth shading (front bright/large, back dim, rim-lit silhouette) with two-axis tumble; GATHER→DIFFUSE→BLOOM arc (radii breathe 1→0.8→1.7); flood carries an opacity-ramped diamond lattice (dedicated overlay div, not background animation); hero title materializes per-char with motes on orb-bloom event only (fixed: EnterStage mounts before intro ends, so dust listens for the event). Verified: build green, 1 warning, dust 37 chars + 14 motes live, 0 errors. Frames VISUALLY VERIFIED (sphere, diamond flood, settled hero).
- [x] Creature orb (research: ferrofluid spikes, siphonophore pulse, SSS interiors): rim tendrils on traveling waves, jellyfish contract/refill asymmetry + metachronal circumference wave, filamented heterogeneous body, white-hot core + cyan sparks, specular glints, single startle contraction at bloom, counter removed (words only). Verified: build green, 1 warning, 0 errors. Frames VISUALLY VERIFIED (spiked surge ball + pool, dust mid-settle).
- [x] Inverted intro (index in front): veil lifts instead of pink flood; orb scatters outward and fades over the revealed field; field ignites via bloom event (packet burst + beacon pulse); index fades in with dust on the living field. Fixed en route: dust word-wrap (spaces outside nowrap words). Verified: build green, 1 warning, scatter + ignited frames, 0 errors.
